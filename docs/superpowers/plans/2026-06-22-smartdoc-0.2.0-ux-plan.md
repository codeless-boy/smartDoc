# smartDoc 0.2.0 — UI/UX 打磨 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 按 [0.2.0 设计文档](../specs/2026-06-22-smartdoc-0.2.0-ux-design.md) 把 smartDoc 从 antd 默认主题升级到"干净克制"中性主题，补齐空状态/加载态/无结果三种 UI、引入键盘快捷键与右键上下文菜单、去除 Electron 默认菜单条；三栏框架与业务逻辑零改动。

**Architecture:** 主题用 antd `ConfigProvider` 的 theme token 集中管理（新建 `theme.ts`）；空状态/快捷键/右键菜单各自封装为独立组件或 hook，AppShell 装配；主进程加一行 `Menu.setApplicationMenu(null)`，仅 dev 模式注册 DevTools 快捷键；复活 0.1.x 移除过的 `file:showInDir` IPC，新增 `file:getAbsolutePath` IPC。

**Tech Stack:** 沿用 0.1.x 全栈（Electron 32 / React 18 / antd 5.20 / Zustand / Vitest / Playwright），不引入新依赖。

**先决条件:** 0.1.0 已发布；master 干净；`npm run lint && npm run format:check && npm run typecheck && npm run test:run` 全绿。在新分支 `feat/0.2.0-ux` 上进行。

---

## 文件结构

新建/修改的文件清单：

| 文件 | 责任 |
|------|------|
| `src/renderer/src/theme.ts` | **新** — antd theme token 单一事实来源 |
| `src/renderer/src/App.tsx` | 改 — 把 theme 传给 ConfigProvider |
| `src/renderer/src/components/EmptyState.tsx` | **新** — 三种空状态（onboarding / no-match / 骨架屏）的单一组件 |
| `src/renderer/src/components/FileContextMenu.tsx` | **新** — 文件行右键菜单 |
| `src/renderer/src/components/TopBar.tsx` | 改 — 共享导入入口提到 hook；样式调整 |
| `src/renderer/src/components/SidePanel.tsx` | 改 — 替换 `<Divider/>` 为细分割；标签云选中态 |
| `src/renderer/src/components/FileTable.tsx` | 改 — 行高/选中色/onContextMenu/EmptyState 切换 |
| `src/renderer/src/components/FileDrawer.tsx` | 改 — 间距/label 字色/打开文件按钮 primary |
| `src/renderer/src/components/DropZone.tsx` | 改 — 遮罩配色去 indigo |
| `src/renderer/src/components/DuplicateDialog.tsx` | 改 — footer 改 Button |
| `src/renderer/src/components/TagChip.tsx` | 改 — 选中态边框 colorPrimary |
| `src/renderer/src/components/file-table.css` | 改 — 删除斑马纹 |
| `src/renderer/src/hooks/use-import.ts` | **新** — 共享导入入口（TopBar + EmptyState + 快捷键） |
| `src/renderer/src/hooks/use-shortcuts.ts` | **新** — 集中管理 6 个键盘快捷键 |
| `src/renderer/src/lib/derive-empty-state.ts` | **新** — 空状态判定纯函数（可单测） |
| `src/renderer/src/lib/derive-esc-action.ts` | **新** — Esc 三级回退纯函数（可单测） |
| `src/renderer/src/store/app-store.ts` | 改 — 新增 `contextMenuTarget` 字段 |
| `src/renderer/src/components/AppShell.tsx` | 改 — 挂载 EmptyState/FileContextMenu/use-shortcuts |
| `src/main/index.ts` | 改 — `Menu.setApplicationMenu(null)`；dev 模式 DevTools 快捷键 |
| `src/shared/ipc-channels.ts` | 改 — 复活 `FileShowInDir`；新增 `FileGetAbsolutePath` |
| `src/main/ipc/file-ipc.ts` | 改 — 复活 showInDir handler；新增 getAbsolutePath handler |
| `src/preload/index.ts` | 改 — 暴露 `file.showInDir` 与 `file.getAbsolutePath` |
| `tests/unit/derive-empty-state.test.ts` | **新** — 空状态判定单测 |
| `tests/unit/derive-esc-action.test.ts` | **新** — Esc 三级回退单测 |
| `tests/e2e/import.spec.ts` | 改 — 选择器还原为 `getByRole('button', ...)` |
| `tests/e2e/context-menu.spec.ts` | **新** — 右键菜单基本路径 E2E |
| `tests/e2e/shortcuts.spec.ts` | **新** — 键盘快捷键基本路径 E2E |
| `README.md` | 改 — 新增"键盘快捷键"小节 |
| `package.json` | 改 — version → 0.2.0 |

---

## Task 1: 切分支 + 升版本号

**Files:**
- Modify: `package.json`

- [ ] **Step 1: 切分支**

```bash
cd /e/code/smartDoc
git checkout master
git pull origin master
git checkout -b feat/0.2.0-ux
```

- [ ] **Step 2: 升 package.json version**

把 `package.json` 里的 `"version": "0.1.0"` 改成 `"version": "0.2.0"`。

- [ ] **Step 3: 跑全套门禁，确认起点干净**

```bash
unset ELECTRON_RUN_AS_NODE  # Git Bash 必备，否则 vitest 会被当成 electron 启动
npm run lint
npm run format:check
npm run typecheck
npm run test:run
```

Expected: 全部 exit 0；test:run 35 通过。

- [ ] **Step 4: 提交**

```bash
git add package.json
git commit -m "chore: 升级版本号到 0.2.0"
```

---

## Task 2: theme.ts —— 中性主题 token

**Files:**
- Create: `src/renderer/src/theme.ts`
- Modify: `src/renderer/src/App.tsx`

- [ ] **Step 1: 创建 `src/renderer/src/theme.ts`**

```ts
import type { ThemeConfig } from 'antd'

/**
 * smartDoc 0.2.0 主题：干净克制（中性灰阶 + 单点墨色强调）。
 * 配色/圆角/字号集中在此；组件内联样式应引用 token，不再硬编码色值。
 */
export const theme: ThemeConfig = {
  token: {
    colorPrimary: '#1f1f1f', // 强调色：墨色（按钮/选中态/链接）
    colorBgLayout: '#f7f8fa', // 页面底
    colorBgContainer: '#ffffff', // 卡片/表格行底
    colorBorder: '#e8e8e8', // 细而淡的分割线
    colorBorderSecondary: '#f0f0f0', // 次级分割线
    colorText: '#1f1f1f',
    colorTextSecondary: '#595959',
    colorTextTertiary: '#8c8c8c',
    borderRadius: 6,
    fontSize: 13, // 比默认 14 略小，紧凑
    controlHeight: 30
  },
  components: {
    Table: {
      headerBg: '#fafafa',
      headerSplitColor: 'transparent',
      rowHoverBg: '#f5f5f5'
    },
    Layout: { siderBg: '#ffffff', headerBg: '#ffffff' },
    Button: { primaryShadow: 'none' },
    Tag: { defaultBg: '#f5f5f5' }
  }
}
```

- [ ] **Step 2: 改 `src/renderer/src/App.tsx`**

把当前 11 行的文件替换为：

```tsx
import { ConfigProvider } from 'antd'
import zhCN from 'antd/locale/zh_CN'
import { AppShell } from './components/AppShell'
import { theme } from './theme'

export function App(): JSX.Element {
  return (
    <ConfigProvider locale={zhCN} theme={theme}>
      <AppShell />
    </ConfigProvider>
  )
}
```

- [ ] **Step 3: typecheck + lint + format**

```bash
npm run typecheck && npm run lint && npm run format:check
```

Expected: 全部 exit 0。

- [ ] **Step 4: 提交**

```bash
git add src/renderer/src/theme.ts src/renderer/src/App.tsx
git commit -m "feat(renderer): 引入 antd theme token，中性主题集中管理配色"
```

---

## Task 3: 复活 file:showInDir + 新增 file:getAbsolutePath（IPC 通道）

**Files:**
- Modify: `src/shared/ipc-channels.ts`
- Modify: `src/main/ipc/file-ipc.ts`
- Modify: `src/preload/index.ts`

- [ ] **Step 1: `src/shared/ipc-channels.ts` 添加两个常量**

打开 `src/shared/ipc-channels.ts`，找到 `FileOpen: 'file:open'` 那一行，在它和 `FileUpdate: 'file:update'` 之间插入：

```ts
  FileShowInDir: 'file:showInDir',
  FileGetAbsolutePath: 'file:getAbsolutePath',
```

(保持其他行不变。)

- [ ] **Step 2: `src/main/ipc/file-ipc.ts` 注册两个 handler**

在 `registerFileIpc` 函数内、`ipcMain.handle(IPC.FileOpenLog, ...)` 之后追加：

```ts
ipcMain.handle(IPC.FileShowInDir, async (_e, id: string): Promise<void> => {
  const file = svc.list({ filter: {} }).find((f) => f.id === id)
  const root = repoRoot()
  if (!file || !root) return
  shell.showItemInFolder(path.join(root, file.storagePath))
})

ipcMain.handle(
  IPC.FileGetAbsolutePath,
  async (_e, id: string): Promise<string | null> => {
    const file = svc.list({ filter: {} }).find((f) => f.id === id)
    const root = repoRoot()
    if (!file || !root) return null
    return path.join(root, file.storagePath)
  }
)
```

(确认顶部 import 已含 `shell` 和 `path`——如缺则补上 `import { shell } from 'electron'` 与 `import path from 'node:path'`。)

- [ ] **Step 3: `src/preload/index.ts` 在 `file` 命名空间内追加两个方法**

在 `file.openLog` 那一行之后插入：

```ts
    showInDir: (id: string): Promise<void> =>
      ipcRenderer.invoke(IPC.FileShowInDir, id),
    getAbsolutePath: (id: string): Promise<string | null> =>
      ipcRenderer.invoke(IPC.FileGetAbsolutePath, id),
```

- [ ] **Step 4: typecheck + test:run**

```bash
npm run typecheck && npm run test:run
```

Expected: typecheck exit 0；test:run 35 通过（主进程改动不影响单测）。

- [ ] **Step 5: 提交**

```bash
git add src/shared/ipc-channels.ts src/main/ipc/file-ipc.ts src/preload/index.ts
git commit -m "feat(ipc): 复活 file:showInDir，新增 file:getAbsolutePath"
```

---

## Task 4: 主进程去掉 Electron 默认菜单 + dev 模式 DevTools 快捷键

**Files:**
- Modify: `src/main/index.ts`

- [ ] **Step 1: 改 import**

把 `src/main/index.ts` 顶部第一行：

```ts
import { app, BrowserWindow } from 'electron'
```

改为：

```ts
import { app, BrowserWindow, Menu } from 'electron'
```

- [ ] **Step 2: 在 `app.whenReady()` 内、`await bootstrap()` 之前插一行**

定位到下面这段（约第 99 行）：

```ts
app.whenReady().then(async () => {
  try {
    await bootstrap()
    createWindow()
    if (updater) updater.init(() => mainWindow)
  } catch (err) {
```

改为：

```ts
app.whenReady().then(async () => {
  try {
    Menu.setApplicationMenu(null) // 去掉默认 File/Edit/View 菜单条
    await bootstrap()
    createWindow()
    if (updater) updater.init(() => mainWindow)
  } catch (err) {
```

- [ ] **Step 3: 在 `createWindow()` 末尾、`if (process.env['ELECTRON_RENDERER_URL'])` 那段之前注册 DevTools 快捷键**

定位到 `createWindow()` 内的最后几行：

```ts
  mainWindow.on('close', () => {
    if (!mainWindow) return
    const b = mainWindow.getBounds()
    setConfig('windowBounds', {
      x: b.x,
      y: b.y,
      width: b.width,
      height: b.height
    })
  })

  if (process.env['ELECTRON_RENDERER_URL']) {
```

在 `mainWindow.on('close', ...)` 那段之后、`if (process.env['ELECTRON_RENDERER_URL'])` 之前插入：

```ts
  // dev 模式保留 DevTools 快捷键（F12 / Ctrl+Shift+I）。
  // 生产构建不注册，避免用户误开。
  if (!app.isPackaged) {
    mainWindow.webContents.on('before-input-event', (event, input) => {
      const isF12 = input.key === 'F12'
      const isCtrlShiftI =
        input.control && input.shift && input.key.toLowerCase() === 'i'
      if (isF12 || isCtrlShiftI) {
        mainWindow?.webContents.toggleDevTools()
        event.preventDefault()
      }
    })
  }

```

- [ ] **Step 4: typecheck + build**

```bash
npm run typecheck
npm run build
```

Expected: 都 exit 0。

- [ ] **Step 5: 提交**

```bash
git add src/main/index.ts
git commit -m "feat(main): 移除默认菜单条；dev 模式保留 DevTools 快捷键"
```

---

## Task 5: TagChip 选中态用 colorPrimary 替代硬编码黑边

**Files:**
- Modify: `src/renderer/src/components/TagChip.tsx`

- [ ] **Step 1: 替换文件全文**

把 `src/renderer/src/components/TagChip.tsx` 替换为：

```tsx
import { Tag, theme as antdTheme } from 'antd'
import type { TagInfo } from '@shared/types'

interface Props {
  tag: TagInfo
  closable?: boolean
  onClose?: () => void
  onClick?: () => void
  selected?: boolean
}

export function TagChip({
  tag,
  closable,
  onClose,
  onClick,
  selected
}: Props): JSX.Element {
  const { token } = antdTheme.useToken()
  return (
    <Tag
      color={tag.color}
      closable={closable}
      onClose={(e) => {
        e.preventDefault()
        onClose?.()
      }}
      onClick={onClick}
      style={{
        cursor: onClick ? 'pointer' : undefined,
        border: selected ? `1px solid ${token.colorPrimary}` : undefined
      }}
    >
      {tag.name}
    </Tag>
  )
}
```

- [ ] **Step 2: typecheck + test:run + lint**

```bash
npm run typecheck && npm run test:run && npm run lint
```

Expected: 全绿。

- [ ] **Step 3: 提交**

```bash
git add src/renderer/src/components/TagChip.tsx
git commit -m "style(TagChip): 选中态边框改用 colorPrimary token"
```

---

## Task 6: FileTable 行高/选中色/斑马纹

**Files:**
- Modify: `src/renderer/src/components/FileTable.tsx`
- Modify: `src/renderer/src/components/file-table.css`

- [ ] **Step 1: 改 file-table.css**

把 `src/renderer/src/components/file-table.css` 全部内容替换为（删除斑马纹规则，只留选中行）：

```css
.row-selected {
  background-color: #f0f0f0 !important;
}
```

- [ ] **Step 2: 改 FileTable.tsx 的 `rowClassName`**

打开 `src/renderer/src/components/FileTable.tsx`，把当前的 `rowClassName` 代码：

```tsx
      rowClassName={(r, idx) =>
        `${r.id === selectedId ? 'row-selected' : ''} ${
          idx % 2 === 0 ? 'row-even' : 'row-odd'
        }`
      }
```

替换为：

```tsx
      rowClassName={(r) => (r.id === selectedId ? 'row-selected' : '')}
```

- [ ] **Step 3: 在 Table 上加 size="small" 收紧行高**

把 `size="middle"` 改为 `size="small"`。

- [ ] **Step 4: typecheck + test:run + e2e**

```bash
npm run typecheck && npm run test:run
npm run test:e2e
```

Expected: typecheck/test:run 全绿；E2E 4 个全过（行高变化不影响选择器）。

- [ ] **Step 5: 提交**

```bash
git add src/renderer/src/components/FileTable.tsx src/renderer/src/components/file-table.css
git commit -m "style(FileTable): 收紧行高、删除斑马纹、选中行用中性灰"
```

---

## Task 7: SidePanel 替换 Divider 为细分割

**Files:**
- Modify: `src/renderer/src/components/SidePanel.tsx`

- [ ] **Step 1: 改 import**

把第 2 行：

```ts
import { Divider, Space, Typography } from 'antd'
```

改为：

```ts
import { Space, Typography, theme as antdTheme } from 'antd'
```

- [ ] **Step 2: 在 SidePanel 函数顶部读 token**

把 `export function SidePanel(): JSX.Element {` 后第一行紧接：

```tsx
  const { token } = antdTheme.useToken()
```

- [ ] **Step 3: 把两处 `<Divider />` 替换为细分割 div**

文件里有两处 `<Divider />`（约第 78 行和第 95 行）。把两处都替换为：

```tsx
      <div
        style={{
          height: 1,
          background: token.colorBorderSecondary,
          margin: '16px 0'
        }}
      />
```

- [ ] **Step 4: typecheck + e2e**

```bash
npm run typecheck && npm run test:e2e
```

Expected: typecheck 0；E2E 4 个全过（侧栏标签点击在 tags-and-search 测试中走 `page.getByText('work', { exact: true }).first()`，不依赖 Divider 结构，应不受影响）。

- [ ] **Step 5: 提交**

```bash
git add src/renderer/src/components/SidePanel.tsx
git commit -m "style(SidePanel): 用细分割 div 替代 antd Divider"
```

---

## Task 8: DropZone 遮罩去 indigo

**Files:**
- Modify: `src/renderer/src/components/DropZone.tsx`

- [ ] **Step 1: 改遮罩 div 的 style**

打开 `src/renderer/src/components/DropZone.tsx`，定位到 `<div data-testid="drop-overlay" ...>` 那段的 style 对象，把当前：

```ts
        background: 'rgba(99, 102, 241, 0.15)',
        border: '4px dashed #6366f1',
```

替换为：

```ts
        background: 'rgba(31, 31, 31, 0.08)',
        border: '4px dashed #1f1f1f',
```

(文字颜色保持默认 `colorText` 即墨色，无需改。)

- [ ] **Step 2: typecheck + lint**

```bash
npm run typecheck && npm run lint
```

- [ ] **Step 3: 提交**

```bash
git add src/renderer/src/components/DropZone.tsx
git commit -m "style(DropZone): 拖拽遮罩配色去 indigo 改中性墨色"
```

---

## Task 9: DuplicateDialog footer 改 Button + 还原 E2E 选择器

**Files:**
- Modify: `src/renderer/src/components/DuplicateDialog.tsx`
- Modify: `tests/e2e/import.spec.ts`

- [ ] **Step 1: 替换 DuplicateDialog.tsx 全文**

```tsx
import { Button, Modal, Space, Typography } from 'antd'
import type { DuplicateAction, FileInfo } from '@shared/types'

interface Props {
  open: boolean
  sourcePath: string
  existing: FileInfo
  onChoose: (action: DuplicateAction) => void
  onCancel: () => void
}

export function DuplicateDialog({
  open,
  sourcePath,
  existing,
  onChoose,
  onCancel
}: Props): JSX.Element {
  return (
    <Modal
      title="文件已存在"
      open={open}
      onCancel={onCancel}
      footer={[
        <Button key="skip" onClick={() => onChoose('skip')}>
          跳过
        </Button>,
        <Button key="overwrite" danger onClick={() => onChoose('overwrite')}>
          覆盖
        </Button>,
        <Button
          key="keep"
          type="primary"
          onClick={() => onChoose('keep-both')}
        >
          保留两份
        </Button>
      ]}
    >
      <Space direction="vertical">
        <Typography.Text>
          仓库中已有同名文件{' '}
          <Typography.Text strong>{existing.name}</Typography.Text>。
        </Typography.Text>
        <Typography.Text type="secondary">源：{sourcePath}</Typography.Text>
      </Space>
    </Modal>
  )
}
```

- [ ] **Step 2: 改 tests/e2e/import.spec.ts —— 还原"保留两份"为 getByRole button**

打开 `tests/e2e/import.spec.ts`，找到这一行（含注释）：

```ts
    await page.getByText('保留两份').click()
```

替换为：

```ts
    await page.getByRole('button', { name: '保留两份' }).click()
```

如果附近有解释性注释（"// antd Modal footer's `<a>` doesn't match `getByRole('button')`"），一并删除——这条限制随 DuplicateDialog 改用 Button 已失效。

- [ ] **Step 3: typecheck + test:run + e2e**

```bash
npm run typecheck && npm run test:run
npm run test:e2e
```

Expected: typecheck/test:run 全绿；E2E 4 个全过（含 import.spec 的"保留两份"路径，现在用 button role 选中）。

- [ ] **Step 4: 提交**

```bash
git add src/renderer/src/components/DuplicateDialog.tsx tests/e2e/import.spec.ts
git commit -m "feat(DuplicateDialog): footer 用 Button 替代 a，还原 E2E 选择器"
```

---

## Task 10: FileDrawer 间距/label 字色/打开文件按钮 primary

**Files:**
- Modify: `src/renderer/src/components/FileDrawer.tsx`

- [ ] **Step 1: import 加 antdTheme**

把 `src/renderer/src/components/FileDrawer.tsx` 顶部 antd 的 import：

```ts
import {
  Button,
  Descriptions,
  Drawer,
  Input,
  Select,
  Space,
  Typography,
  message
} from 'antd'
```

改为：

```ts
import {
  Button,
  Descriptions,
  Drawer,
  Input,
  Select,
  Space,
  Typography,
  message,
  theme as antdTheme
} from 'antd'
```

- [ ] **Step 2: 在组件函数顶部读 token**

把 `export function FileDrawer(): JSX.Element {` 后第一行紧接：

```tsx
  const { token } = antdTheme.useToken()
```

- [ ] **Step 3: 把外层 Space 的 size 调大**

定位到 `<Drawer ...>` 内的第一个 `<Space direction="vertical" ...>`，把它改为：

```tsx
      <Space direction="vertical" size={20} style={{ width: '100%' }}>
```

- [ ] **Step 4: 把 Descriptions.Item 的 label 用次级字色包一层**

找到 Descriptions 块：

```tsx
        <Descriptions column={1} size="small">
          <Descriptions.Item label="大小">{file.size} B</Descriptions.Item>
          <Descriptions.Item label="导入时间">
            {file.importedAt}
          </Descriptions.Item>
          <Descriptions.Item label="路径">{file.storagePath}</Descriptions.Item>
        </Descriptions>
```

替换为：

```tsx
        <Descriptions
          column={1}
          size="small"
          labelStyle={{ color: token.colorTextTertiary, fontSize: 12 }}
        >
          <Descriptions.Item label="大小">{file.size} B</Descriptions.Item>
          <Descriptions.Item label="导入时间">
            {file.importedAt}
          </Descriptions.Item>
          <Descriptions.Item label="路径">{file.storagePath}</Descriptions.Item>
        </Descriptions>
```

- [ ] **Step 5: 打开文件按钮加 type="primary"**

找到 "打开文件" 按钮：

```tsx
          <Button
            icon={<FileOutlined />}
            onClick={() => window.api.file.open(file.id)}
          >
            打开文件
          </Button>
```

改为：

```tsx
          <Button
            type="primary"
            icon={<FileOutlined />}
            onClick={() => window.api.file.open(file.id)}
          >
            打开文件
          </Button>
```

- [ ] **Step 6: typecheck + e2e**

```bash
npm run typecheck && npm run test:e2e
```

Expected: typecheck 0；E2E 4 个全过。

- [ ] **Step 7: 提交**

```bash
git add src/renderer/src/components/FileDrawer.tsx
git commit -m "style(FileDrawer): 间距加大、label 用 colorTextTertiary、打开文件按钮 primary"
```

---

## Task 11: use-import.ts 共享导入入口

**Files:**
- Create: `src/renderer/src/hooks/use-import.ts`
- Modify: `src/renderer/src/components/TopBar.tsx`

把 TopBar 内部的 `pickAndImport` / `handleImport` 提到一个 hook，供 TopBar 按钮、EmptyState onboarding 按钮、键盘快捷键 Ctrl+I 共享。`window.__smartdoc_dupResolver` 这个 hack 保持原样（Part 2 末尾的设计决定，不改）。

- [ ] **Step 1: 创建 `src/renderer/src/hooks/use-import.ts`**

```ts
import { useCallback, useState } from 'react'
import type { FileInfo, ImportItemStatus } from '@shared/types'
import { refreshAll } from '@renderer/api/use-files'

interface DupTarget {
  sourcePath: string
  existing: FileInfo
}

/**
 * 共享的导入入口：弹文件选择器 → 逐个文件导入 → 重名时等待外部 resolver。
 *
 * 调用方（TopBar / EmptyState / 快捷键）共用同一函数；重名对话框由 TopBar 渲染，
 * 通过 `window.__smartdoc_dupResolver` 桥接 Promise（沿用 0.1.x 的方案）。
 */
export function useImport(): {
  pickAndImport: () => Promise<void>
  dup: DupTarget | null
  setDup: (d: DupTarget | null) => void
} {
  const [dup, setDup] = useState<DupTarget | null>(null)

  const pickAndImport = useCallback(async (): Promise<void> => {
    const paths = await window.api.dialog.pickFiles()
    if (paths.length === 0) return

    for (const p of paths) {
      let result: ImportItemStatus = await window.api.file.import({
        sourcePath: p
      })
      if (result.status === 'duplicate') {
        const action = await new Promise<'skip' | 'overwrite' | 'keep-both'>(
          (resolve) => {
            setDup({ sourcePath: result.sourcePath, existing: result.existing })
            ;(window as any).__smartdoc_dupResolver = resolve
          }
        )
        setDup(null)
        result = await window.api.file.import({
          sourcePath: p,
          duplicateAction: action
        })
      }
    }
    await refreshAll()
  }, [])

  return { pickAndImport, dup, setDup }
}
```

(`result.sourcePath` 与 `result.existing` 在 `result.status === 'duplicate'` 内通过 TS 判别联合自动收窄类型——比 0.1.x TopBar 里 `result.status === 'duplicate' ? ... : ({} as never)` 那段 hack 更干净。)

- [ ] **Step 2: 改 TopBar.tsx**

把当前 110 行的 `src/renderer/src/components/TopBar.tsx` 替换为：

```tsx
import { useEffect, useMemo, useRef, useState } from 'react'
import { AutoComplete, Button, Input, Space } from 'antd'
import { ImportOutlined } from '@ant-design/icons'
import { useAppStore } from '@renderer/store/app-store'
import { useImport } from '@renderer/hooks/use-import'
import { DuplicateDialog } from './DuplicateDialog'

export function TopBar(): JSX.Element {
  const setKeyword = useAppStore((s) => s.setKeyword)
  const [draft, setDraft] = useState('')
  const [options, setOptions] = useState<{ value: string; label: string }[]>([])
  const debounceRef = useRef<ReturnType<typeof setTimeout> | null>(null)
  const { pickAndImport, dup, setDup } = useImport()

  // 输入 → 300ms 防抖 → 写入 store.keyword 触发列表刷新
  useEffect(() => {
    if (debounceRef.current) clearTimeout(debounceRef.current)
    debounceRef.current = setTimeout(() => {
      setKeyword(draft.trim())
    }, 300)
    return () => {
      if (debounceRef.current) clearTimeout(debounceRef.current)
    }
  }, [draft, setKeyword])

  const onSuggest = useMemo(
    () =>
      async (value: string): Promise<void> => {
        if (!value) {
          setOptions([])
          return
        }
        const list = await window.api.search.suggest(value)
        setOptions(
          list.map((s) => ({
            value: s.text,
            label: `${s.kind === 'tag' ? '🏷️ ' : ''}${s.text}`
          }))
        )
      },
    []
  )

  return (
    <Space style={{ padding: 12, width: '100%' }}>
      <AutoComplete
        options={options}
        onSearch={onSuggest}
        onSelect={(v) => setDraft(v)}
        style={{ width: 480 }}
      >
        <Input.Search
          placeholder="搜索文件名 / 标签 / 备注"
          value={draft}
          onChange={(e) => setDraft(e.target.value)}
          allowClear
          data-testid="search-input"
        />
      </AutoComplete>
      <Button type="primary" icon={<ImportOutlined />} onClick={pickAndImport}>
        导入文件
      </Button>
      {dup && (
        <DuplicateDialog
          open
          sourcePath={dup.sourcePath}
          existing={dup.existing}
          onChoose={(a) => {
            ;(window as any).__smartdoc_dupResolver?.(a)
            setDup(null)
          }}
          onCancel={() => {
            ;(window as any).__smartdoc_dupResolver?.('skip')
            setDup(null)
          }}
        />
      )}
    </Space>
  )
}
```

关键变化：
- `pickAndImport` 来自 hook，TopBar 不再包含导入循环代码
- 搜索框加 `data-testid="search-input"`（供 Task 14 快捷键 Ctrl+F 用 querySelector 命中）
- 删掉 0.1.x 的 `SearchSuggestion extends never ? never : ...` 那段 hack，因为 `dup` 现在是 hook 返回的 `DupTarget | null`，类型清楚

- [ ] **Step 3: typecheck + lint + e2e**

```bash
npm run typecheck && npm run lint
npm run test:e2e
```

Expected: 全绿；4 个 E2E 通过（共享导入入口对 E2E 来说语义不变）。

- [ ] **Step 4: 提交**

```bash
git add src/renderer/src/hooks/use-import.ts src/renderer/src/components/TopBar.tsx
git commit -m "refactor(import): 提取 useImport hook 作为共享导入入口"
```

---

## Task 12: 空状态判定纯函数 + 单测（TDD）

**Files:**
- Create: `src/renderer/src/lib/derive-empty-state.ts`
- Test: `tests/unit/derive-empty-state.test.ts`

把 EmptyState 的判定逻辑（哪种空状态、活跃筛选条件描述）抽成纯函数。先写测试再写实现。

- [ ] **Step 1: 写测试 `tests/unit/derive-empty-state.test.ts`**

```ts
import { describe, it, expect } from 'vitest'
import { deriveEmptyState } from '@renderer/lib/derive-empty-state'
import type { FileFilter, FileWithTags } from '@shared/types'

const noFiles: FileWithTags[] = []
const oneFile: FileWithTags[] = [
  {
    id: 'a',
    name: 'a.pdf',
    ext: 'pdf',
    size: 1,
    storagePath: 'files/a/a.pdf',
    note: '',
    importedAt: 't',
    updatedAt: 't',
    tagIds: []
  }
]
const emptyFilter: FileFilter = {}

describe('deriveEmptyState', () => {
  it('returns null when files non-empty', () => {
    expect(deriveEmptyState(oneFile, false, emptyFilter)).toBeNull()
  })

  it('returns loading when loading=true regardless of files', () => {
    expect(deriveEmptyState(noFiles, true, emptyFilter)).toEqual({
      kind: 'loading'
    })
    expect(deriveEmptyState(oneFile, true, emptyFilter)).toEqual({
      kind: 'loading'
    })
  })

  it('returns onboarding when empty + empty filter', () => {
    expect(deriveEmptyState(noFiles, false, emptyFilter)).toEqual({
      kind: 'onboarding'
    })
  })

  it('returns no-match with keyword summary', () => {
    const r = deriveEmptyState(noFiles, false, { keyword: 'foo' })
    expect(r).toEqual({
      kind: 'no-match',
      activeFilters: ['关键词: "foo"']
    })
  })

  it('returns no-match with multiple filter summary', () => {
    const r = deriveEmptyState(noFiles, false, {
      keyword: 'foo',
      exts: ['pdf', 'png'],
      untagged: true,
      tagIds: ['t1', 't2']
    })
    expect(r?.kind).toBe('no-match')
    expect(r?.activeFilters).toEqual([
      '关键词: "foo"',
      '类型: pdf, png',
      '标签: 2 个',
      '未打标签'
    ])
  })

  it('treats topOpenedLimit as active filter', () => {
    const r = deriveEmptyState(noFiles, false, { topOpenedLimit: 20 })
    expect(r).toEqual({
      kind: 'no-match',
      activeFilters: ['常用文档（前 20）']
    })
  })
})
```

- [ ] **Step 2: 运行测试，确认失败**

```bash
npm run test:run -- tests/unit/derive-empty-state.test.ts
```

Expected: FAIL（模块不存在）。

- [ ] **Step 3: 实现 `src/renderer/src/lib/derive-empty-state.ts`**

```ts
import type { FileFilter, FileWithTags } from '@shared/types'

export type EmptyStateKind =
  | { kind: 'loading' }
  | { kind: 'onboarding' }
  | { kind: 'no-match'; activeFilters: string[] }

/**
 * 根据当前 files / loading / filter 决定该渲染什么空状态；
 * 返回 null 表示不该渲染空状态（有数据）。
 *
 * 优先级：loading > files 非空 > 有活跃 filter → no-match > onboarding。
 */
export function deriveEmptyState(
  files: readonly FileWithTags[],
  loading: boolean,
  filter: FileFilter
): EmptyStateKind | null {
  if (loading) return { kind: 'loading' }
  if (files.length > 0) return null

  const activeFilters = summarizeFilter(filter)
  if (activeFilters.length === 0) return { kind: 'onboarding' }
  return { kind: 'no-match', activeFilters }
}

function summarizeFilter(f: FileFilter): string[] {
  const out: string[] = []
  if (f.keyword) out.push(`关键词: "${f.keyword}"`)
  if (f.exts && f.exts.length > 0) out.push(`类型: ${f.exts.join(', ')}`)
  if (f.tagIds && f.tagIds.length > 0) out.push(`标签: ${f.tagIds.length} 个`)
  if (f.untagged) out.push('未打标签')
  if (f.topOpenedLimit) out.push(`常用文档（前 ${f.topOpenedLimit}）`)
  return out
}
```

- [ ] **Step 4: 运行测试，确认通过**

```bash
npm run test:run -- tests/unit/derive-empty-state.test.ts
```

Expected: 6 个用例 PASS。

- [ ] **Step 5: 全量回归**

```bash
npm run test:run
```

Expected: 41 个测试通过（35 + 6 新增）。

- [ ] **Step 6: 提交**

```bash
git add src/renderer/src/lib/derive-empty-state.ts tests/unit/derive-empty-state.test.ts
git commit -m "feat(empty-state): 空状态判定纯函数 + 6 个单测"
```

---

## Task 13: EmptyState 组件 + 接入 FileTable

**Files:**
- Create: `src/renderer/src/components/EmptyState.tsx`
- Modify: `src/renderer/src/components/FileTable.tsx`
- Modify: `src/renderer/src/components/AppShell.tsx`

注意：`useImport` hook 是状态化的，每次调用都创建独立的 `dup` 状态。当前 TopBar 已经用了 hook，如果 EmptyState 又调一次，会有两套 `dup` 状态——只一处能渲染对话框。解决：把 hook 调用上提到 AppShell，通过 props 把 `pickAndImport` 透传给 TopBar 和 FileTable。

- [ ] **Step 1: 改 AppShell.tsx —— 在顶层调 useImport，把 pickAndImport 透传**

把 `src/renderer/src/components/AppShell.tsx` 整个替换为：

```tsx
import { useEffect, useState } from 'react'
import { Layout } from 'antd'
import type { DuplicateAction, FileInfo } from '@shared/types'
import { useFiles } from '@renderer/api/use-files'
import { useImport } from '@renderer/hooks/use-import'
import { TopBar } from './TopBar'
import { SidePanel } from './SidePanel'
import { FileTable } from './FileTable'
import { FileDrawer } from './FileDrawer'
import { DropZone } from './DropZone'
import { DuplicateDialog } from './DuplicateDialog'
import { FirstRunGuide } from './FirstRunGuide'
import { UpdateNotifier } from './UpdateNotifier'
import { ImportProgress } from './ImportProgress'

export function AppShell(): JSX.Element {
  useFiles()
  const [repoReady, setRepoReady] = useState<boolean | null>(null)

  useEffect(() => {
    void window.api.config.getAll().then((c) => setRepoReady(!!c.repoPath))
  }, [])

  // 共享的导入入口：TopBar 按钮、EmptyState onboarding 按钮、键盘 Ctrl+I 共用。
  const { pickAndImport, dup: importDup, setDup: setImportDup } = useImport()

  // DropZone 的重名对话框走另一条路径（Promise 直接 resolve），保持原状。
  const [dropDup, setDropDup] = useState<{
    sourcePath: string
    existing: FileInfo
    resolve: (a: DuplicateAction) => void
  } | null>(null)

  function askDuplicate(
    sourcePath: string,
    existing: FileInfo
  ): Promise<DuplicateAction> {
    return new Promise((resolve) =>
      setDropDup({ sourcePath, existing, resolve })
    )
  }

  if (repoReady === null) return <></>
  if (!repoReady) return <FirstRunGuide onChosen={() => setRepoReady(true)} />

  return (
    <Layout style={{ height: '100vh' }}>
      <Layout.Header
        style={{
          padding: 0,
          borderBottom: '1px solid #e8e8e8',
          height: 48,
          lineHeight: '48px'
        }}
      >
        <TopBar pickAndImport={pickAndImport} />
      </Layout.Header>
      <Layout>
        <Layout.Sider width={220}>
          <SidePanel />
        </Layout.Sider>
        <Layout.Content style={{ overflow: 'auto', padding: 12 }}>
          <FileTable pickAndImport={pickAndImport} />
        </Layout.Content>
      </Layout>
      <FileDrawer />
      <DropZone onDuplicate={askDuplicate} />
      {importDup && (
        <DuplicateDialog
          open
          sourcePath={importDup.sourcePath}
          existing={importDup.existing}
          onChoose={(a) => {
            ;(window as any).__smartdoc_dupResolver?.(a)
            setImportDup(null)
          }}
          onCancel={() => {
            ;(window as any).__smartdoc_dupResolver?.('skip')
            setImportDup(null)
          }}
        />
      )}
      {dropDup && (
        <DuplicateDialog
          open
          sourcePath={dropDup.sourcePath}
          existing={dropDup.existing}
          onChoose={(a) => {
            dropDup.resolve(a)
            setDropDup(null)
          }}
          onCancel={() => {
            dropDup.resolve('skip')
            setDropDup(null)
          }}
        />
      )}
      <UpdateNotifier />
      <ImportProgress />
    </Layout>
  )
}
```

关键变化：
- `useImport` 在 AppShell 顶层调一次，`pickAndImport` 通过 props 透传给 TopBar 和 FileTable
- DropZone 仍走 askDuplicate 路径（它的对话框语义不同，独立 state）
- Header 高度收到 48px，底边用 token 色

- [ ] **Step 2: 改 TopBar.tsx —— 接收 props**

把 `export function TopBar(): JSX.Element {` 改为：

```tsx
interface Props {
  pickAndImport: () => Promise<void>
}

export function TopBar({ pickAndImport }: Props): JSX.Element {
```

并删除文件内的 `import { useImport } from '@renderer/hooks/use-import'`、`const { pickAndImport, dup, setDup } = useImport()` 以及它对应渲染的 `{dup && (...)}` Modal——这些现在归 AppShell。

完整替换 TopBar.tsx 为：

```tsx
import { useEffect, useMemo, useRef, useState } from 'react'
import { AutoComplete, Button, Input, Space } from 'antd'
import { ImportOutlined } from '@ant-design/icons'
import { useAppStore } from '@renderer/store/app-store'

interface Props {
  pickAndImport: () => Promise<void>
}

export function TopBar({ pickAndImport }: Props): JSX.Element {
  const setKeyword = useAppStore((s) => s.setKeyword)
  const [draft, setDraft] = useState('')
  const [options, setOptions] = useState<{ value: string; label: string }[]>([])
  const debounceRef = useRef<ReturnType<typeof setTimeout> | null>(null)

  useEffect(() => {
    if (debounceRef.current) clearTimeout(debounceRef.current)
    debounceRef.current = setTimeout(() => {
      setKeyword(draft.trim())
    }, 300)
    return () => {
      if (debounceRef.current) clearTimeout(debounceRef.current)
    }
  }, [draft, setKeyword])

  const onSuggest = useMemo(
    () =>
      async (value: string): Promise<void> => {
        if (!value) {
          setOptions([])
          return
        }
        const list = await window.api.search.suggest(value)
        setOptions(
          list.map((s) => ({
            value: s.text,
            label: `${s.kind === 'tag' ? '🏷️ ' : ''}${s.text}`
          }))
        )
      },
    []
  )

  return (
    <Space style={{ padding: 12, width: '100%' }}>
      <AutoComplete
        options={options}
        onSearch={onSuggest}
        onSelect={(v) => setDraft(v)}
        style={{ width: 480 }}
      >
        <Input.Search
          placeholder="搜索文件名 / 标签 / 备注"
          value={draft}
          onChange={(e) => setDraft(e.target.value)}
          allowClear
          data-testid="search-input"
        />
      </AutoComplete>
      <Button type="primary" icon={<ImportOutlined />} onClick={pickAndImport}>
        导入文件
      </Button>
    </Space>
  )
}
```

- [ ] **Step 3: 创建 `src/renderer/src/components/EmptyState.tsx`**

```tsx
import { Button, Empty, Skeleton, Typography } from 'antd'
import type { EmptyStateKind } from '@renderer/lib/derive-empty-state'
import { useAppStore } from '@renderer/store/app-store'

interface Props {
  state: EmptyStateKind
  onImport: () => void
}

export function EmptyState({ state, onImport }: Props): JSX.Element {
  if (state.kind === 'loading') {
    return (
      <div data-testid="empty-loading" style={{ padding: 16 }}>
        {[0, 1, 2, 3, 4, 5].map((i) => (
          <Skeleton.Input
            key={i}
            active
            block
            style={{ marginBottom: 8, height: 32 }}
          />
        ))}
      </div>
    )
  }

  if (state.kind === 'onboarding') {
    return (
      <div
        data-testid="empty-onboarding"
        style={{
          display: 'flex',
          flexDirection: 'column',
          alignItems: 'center',
          justifyContent: 'center',
          minHeight: 400,
          gap: 16
        }}
      >
        <Empty
          image={Empty.PRESENTED_IMAGE_SIMPLE}
          description={
            <div>
              <Typography.Title level={5} style={{ marginBottom: 4 }}>
                还没有文件
              </Typography.Title>
              <Typography.Text type="secondary">
                把文件拖进来，或点下方按钮选择
              </Typography.Text>
            </div>
          }
        />
        <Button type="primary" onClick={onImport}>
          导入第一个文件
        </Button>
      </div>
    )
  }

  // no-match
  return (
    <div
      data-testid="empty-no-match"
      style={{
        display: 'flex',
        flexDirection: 'column',
        alignItems: 'center',
        justifyContent: 'center',
        minHeight: 400,
        gap: 12
      }}
    >
      <Empty
        image={Empty.PRESENTED_IMAGE_SIMPLE}
        description={
          <div>
            <Typography.Title level={5} style={{ marginBottom: 4 }}>
              没有匹配的文件
            </Typography.Title>
            <Typography.Text type="secondary">
              当前筛选：{state.activeFilters.join('，')}
            </Typography.Text>
          </div>
        }
      />
      <Button
        type="link"
        onClick={() => useAppStore.getState().resetFilter()}
      >
        清除筛选
      </Button>
    </div>
  )
}
```

- [ ] **Step 4: 改 FileTable.tsx —— 接收 pickAndImport + 切换 EmptyState**

把整个 `src/renderer/src/components/FileTable.tsx` 替换为：

```tsx
import { Space, Table } from 'antd'
import type { ColumnsType } from 'antd/es/table'
import type { FileWithTags } from '@shared/types'
import { useAppStore } from '@renderer/store/app-store'
import { deriveEmptyState } from '@renderer/lib/derive-empty-state'
import { fileIconFor } from './file-icon'
import { TagChip } from './TagChip'
import { EmptyState } from './EmptyState'
import './file-table.css'

function formatSize(bytes: number): string {
  if (bytes < 1024) return `${bytes} B`
  if (bytes < 1024 * 1024) return `${(bytes / 1024).toFixed(1)} KB`
  if (bytes < 1024 * 1024 * 1024)
    return `${(bytes / 1024 / 1024).toFixed(1)} MB`
  return `${(bytes / 1024 / 1024 / 1024).toFixed(2)} GB`
}

function formatDate(iso: string): string {
  return iso.slice(0, 10)
}

interface Props {
  pickAndImport: () => Promise<void>
}

export function FileTable({ pickAndImport }: Props): JSX.Element {
  const files = useAppStore((s) => s.files)
  const tags = useAppStore((s) => s.tags)
  const filter = useAppStore((s) => s.filter)
  const select = useAppStore((s) => s.select)
  const selectedId = useAppStore((s) => s.selectedId)
  const loading = useAppStore((s) => s.loading)

  const empty = deriveEmptyState(files, loading, filter)
  if (empty) {
    return <EmptyState state={empty} onImport={pickAndImport} />
  }

  const tagsById = new Map(tags.map((t) => [t.id, t] as const))

  const columns: ColumnsType<FileWithTags> = [
    {
      title: '文件名',
      key: 'name',
      render: (_v, record) => (
        <Space>
          {fileIconFor(record.ext)}
          <span>{record.name}</span>
          {record.tagIds.map((tid) => {
            const t = tagsById.get(tid)
            return t ? <TagChip key={tid} tag={t} /> : null
          })}
        </Space>
      )
    },
    {
      title: '大小',
      key: 'size',
      width: 100,
      render: (_v, r) => formatSize(r.size)
    },
    {
      title: '导入时间',
      key: 'importedAt',
      width: 120,
      render: (_v, r) => formatDate(r.importedAt)
    }
  ]

  return (
    <Table<FileWithTags>
      rowKey="id"
      columns={columns}
      dataSource={files}
      pagination={false}
      size="small"
      data-testid="file-table"
      rowClassName={(r) => (r.id === selectedId ? 'row-selected' : '')}
      onRow={(record) => ({
        onClick: () => select(record.id),
        onDoubleClick: async () => {
          const ok = await window.api.file.existsOnDisk(record.id)
          if (!ok) {
            const { message } = await import('antd')
            message.error('文件已丢失，无法打开')
            return
          }
          void window.api.file.open(record.id)
        }
      })}
    />
  )
}
```

关键变化：
- 接收 `pickAndImport` prop
- 用 `deriveEmptyState` 决定渲染 EmptyState 还是 Table
- 移除 `loading={loading}`——Skeleton 已在 EmptyState 里渲染
- `size="middle"` → `size="small"`（Task 6 的延续）
- `rowClassName` 只剩选中态（Task 6 的延续）

- [ ] **Step 5: typecheck + test:run + e2e**

```bash
npm run typecheck && npm run test:run
npm run test:e2e
```

Expected: 全绿。

> **可能的 E2E 影响**：tags-and-search.spec.ts 的 beforeEach 先导入 3 个文件再点标签等。导入完成后 files.length > 0，EmptyState 不再渲染，Table 正常出现，选择器 `getByTestId('file-table')` 仍能命中。应不受影响。

- [ ] **Step 6: 提交**

```bash
git add src/renderer/src/components/EmptyState.tsx src/renderer/src/components/AppShell.tsx src/renderer/src/components/TopBar.tsx src/renderer/src/components/FileTable.tsx
git commit -m "feat(empty-state): 三种空状态 UI（onboarding / no-match / 骨架屏）"
```

---

## Task 14: Esc 三级回退纯函数 + 单测（TDD）

**Files:**
- Create: `src/renderer/src/lib/derive-esc-action.ts`
- Test: `tests/unit/derive-esc-action.test.ts`

- [ ] **Step 1: 写测试 `tests/unit/derive-esc-action.test.ts`**

```ts
import { describe, it, expect } from 'vitest'
import { deriveEscAction } from '@renderer/lib/derive-esc-action'

describe('deriveEscAction', () => {
  it('returns close-drawer when selectedId is set (highest priority)', () => {
    expect(
      deriveEscAction({
        selectedId: 'a',
        keyword: 'foo',
        hasFilter: true
      })
    ).toBe('close-drawer')
  })

  it('returns clear-keyword when no selection but keyword non-empty', () => {
    expect(
      deriveEscAction({
        selectedId: null,
        keyword: 'foo',
        hasFilter: true
      })
    ).toBe('clear-keyword')
  })

  it('returns reset-filter when no selection no keyword but filter active', () => {
    expect(
      deriveEscAction({
        selectedId: null,
        keyword: '',
        hasFilter: true
      })
    ).toBe('reset-filter')
  })

  it('returns null when nothing to do', () => {
    expect(
      deriveEscAction({
        selectedId: null,
        keyword: '',
        hasFilter: false
      })
    ).toBeNull()
  })
})
```

- [ ] **Step 2: 运行测试，确认失败**

```bash
npm run test:run -- tests/unit/derive-esc-action.test.ts
```

Expected: FAIL（模块不存在）。

- [ ] **Step 3: 实现 `src/renderer/src/lib/derive-esc-action.ts`**

```ts
export type EscAction = 'close-drawer' | 'clear-keyword' | 'reset-filter' | null

interface State {
  selectedId: string | null
  keyword: string
  hasFilter: boolean
}

/**
 * Esc 键三级回退：先关抽屉 → 再清搜索 → 再重置筛选。
 * 任何时刻只触发优先级最高的那一级。
 */
export function deriveEscAction(s: State): EscAction {
  if (s.selectedId) return 'close-drawer'
  if (s.keyword) return 'clear-keyword'
  if (s.hasFilter) return 'reset-filter'
  return null
}
```

- [ ] **Step 4: 运行测试，确认通过**

```bash
npm run test:run -- tests/unit/derive-esc-action.test.ts
```

Expected: 4 个 PASS。

- [ ] **Step 5: 提交**

```bash
git add src/renderer/src/lib/derive-esc-action.ts tests/unit/derive-esc-action.test.ts
git commit -m "feat(shortcuts): Esc 三级回退判定纯函数 + 4 个单测"
```

---

## Task 15: use-shortcuts hook —— 6 个键盘快捷键

**Files:**
- Create: `src/renderer/src/hooks/use-shortcuts.ts`
- Modify: `src/renderer/src/components/AppShell.tsx`

- [ ] **Step 1: 创建 `src/renderer/src/hooks/use-shortcuts.ts`**

```ts
import { useEffect } from 'react'
import { Modal } from 'antd'
import { useAppStore } from '@renderer/store/app-store'
import { refreshAll } from '@renderer/api/use-files'
import { deriveEscAction } from '@renderer/lib/derive-esc-action'

/**
 * 集中管理全局键盘快捷键。在 AppShell 顶层调一次。
 *
 * 输入框/textarea 内按键 **不**触发全局快捷键（Esc 例外）。
 * 见 https://github.com/<owner>/smartDoc/docs/superpowers/specs/2026-06-22-...md §6
 */
export function useShortcuts(pickAndImport: () => Promise<void>): void {
  useEffect(() => {
    function inEditable(t: EventTarget | null): boolean {
      if (!(t instanceof HTMLElement)) return false
      const tag = t.tagName
      if (tag === 'INPUT' || tag === 'TEXTAREA') return true
      if (t.isContentEditable) return true
      return false
    }

    function handler(e: KeyboardEvent): void {
      const editable = inEditable(e.target)

      // Esc 任何场景都响应（即使在输入框）
      if (e.key === 'Escape') {
        const s = useAppStore.getState()
        const action = deriveEscAction({
          selectedId: s.selectedId,
          keyword: s.keyword,
          hasFilter: hasActiveFilter(s.filter)
        })
        if (action === 'close-drawer') s.select(null)
        else if (action === 'clear-keyword') s.setKeyword('')
        else if (action === 'reset-filter') s.resetFilter()
        if (action) e.preventDefault()
        return
      }

      // 其它快捷键在编辑态时不响应
      if (editable) return

      // Ctrl+F 或 /  → 聚焦搜索框
      if ((e.ctrlKey && e.key.toLowerCase() === 'f') || e.key === '/') {
        const el = document.querySelector<HTMLInputElement>(
          '[data-testid="search-input"]'
        )
        if (el) {
          el.focus()
          el.select()
          e.preventDefault()
        }
        return
      }

      // Ctrl+I  → 触发导入
      if (e.ctrlKey && e.key.toLowerCase() === 'i') {
        e.preventDefault()
        void pickAndImport()
        return
      }

      // Delete  → 删除当前选中（带确认）
      if (e.key === 'Delete') {
        const s = useAppStore.getState()
        if (!s.selectedId) return
        const file = s.files.find((f) => f.id === s.selectedId)
        if (!file) return
        e.preventDefault()
        Modal.confirm({
          title: '删除文件',
          content: `确认删除「${file.name}」？此操作会同时移除磁盘文件。`,
          okText: '删除',
          okButtonProps: { danger: true },
          cancelText: '取消',
          onOk: async () => {
            await window.api.file.delete([file.id])
            useAppStore.getState().select(null)
            await refreshAll()
          }
        })
        return
      }

      // Enter  → 打开当前选中
      if (e.key === 'Enter') {
        const s = useAppStore.getState()
        if (!s.selectedId) return
        e.preventDefault()
        void window.api.file.open(s.selectedId)
        return
      }

      // ↑ / ↓  → 在文件列表中上下移动选中行
      if (e.key === 'ArrowUp' || e.key === 'ArrowDown') {
        const s = useAppStore.getState()
        if (s.files.length === 0) return
        e.preventDefault()
        const idx = s.files.findIndex((f) => f.id === s.selectedId)
        let next: number
        if (idx < 0) next = e.key === 'ArrowDown' ? 0 : s.files.length - 1
        else next = idx + (e.key === 'ArrowDown' ? 1 : -1)
        next = Math.max(0, Math.min(s.files.length - 1, next))
        s.select(s.files[next].id)
      }
    }

    window.addEventListener('keydown', handler)
    return () => window.removeEventListener('keydown', handler)
  }, [pickAndImport])
}

function hasActiveFilter(f: ReturnType<typeof useAppStore.getState>['filter']): boolean {
  return !!(
    f.keyword ||
    (f.tagIds && f.tagIds.length > 0) ||
    (f.exts && f.exts.length > 0) ||
    f.untagged ||
    f.topOpenedLimit
  )
}
```

- [ ] **Step 2: 改 AppShell.tsx —— 调用 useShortcuts**

打开 `src/renderer/src/components/AppShell.tsx`，在 import 区添加：

```ts
import { useShortcuts } from '@renderer/hooks/use-shortcuts'
```

在 `const { pickAndImport, dup: importDup, setDup: setImportDup } = useImport()` 之后立刻调：

```ts
  useShortcuts(pickAndImport)
```

- [ ] **Step 3: typecheck + test:run**

```bash
npm run typecheck && npm run test:run
```

Expected: typecheck 0；test:run 全过（45 个：原 35 + Task 12 的 6 + Task 14 的 4）。

- [ ] **Step 4: 提交**

```bash
git add src/renderer/src/hooks/use-shortcuts.ts src/renderer/src/components/AppShell.tsx
git commit -m "feat(shortcuts): 6 个键盘快捷键（Ctrl+F / Esc / Delete / Ctrl+I / Enter / 上下键）"
```

---

## Task 16: 扩展 Zustand store —— contextMenuTarget

**Files:**
- Modify: `src/renderer/src/store/app-store.ts`

- [ ] **Step 1: 替换 store 全文**

把 `src/renderer/src/store/app-store.ts` 整个替换为：

```ts
import { create } from 'zustand'
import type { FileFilter, FileWithTags, TagInfo } from '@shared/types'

interface ContextMenuTarget {
  id: string
  x: number
  y: number
}

interface AppState {
  // 数据
  files: FileWithTags[]
  tags: TagInfo[]

  // 筛选 / 搜索
  filter: FileFilter
  keyword: string

  // 选中
  selectedId: string | null

  // 加载状态
  loading: boolean

  // 右键菜单（0.2.0 新增）
  contextMenuTarget: ContextMenuTarget | null

  // actions
  setFiles: (files: FileWithTags[]) => void
  setTags: (tags: TagInfo[]) => void
  setKeyword: (kw: string) => void
  patchFilter: (patch: Partial<FileFilter>) => void
  resetFilter: () => void
  select: (id: string | null) => void
  setLoading: (b: boolean) => void
  openContextMenu: (target: ContextMenuTarget) => void
  closeContextMenu: () => void
}

export const useAppStore = create<AppState>((set) => ({
  files: [],
  tags: [],
  filter: {},
  keyword: '',
  selectedId: null,
  loading: false,
  contextMenuTarget: null,

  setFiles: (files) => set({ files }),
  setTags: (tags) => set({ tags }),
  setKeyword: (keyword) =>
    set((s) => ({ keyword, filter: { ...s.filter, keyword } })),
  patchFilter: (patch) => set((s) => ({ filter: { ...s.filter, ...patch } })),
  resetFilter: () => set({ filter: {}, keyword: '' }),
  select: (selectedId) => set({ selectedId }),
  setLoading: (loading) => set({ loading }),
  openContextMenu: (target) => set({ contextMenuTarget: target }),
  closeContextMenu: () => set({ contextMenuTarget: null })
}))
```

- [ ] **Step 2: typecheck + test:run**

```bash
npm run typecheck && npm run test:run
```

Expected: 全绿。

- [ ] **Step 3: 提交**

```bash
git add src/renderer/src/store/app-store.ts
git commit -m "feat(store): 新增 contextMenuTarget 字段，供右键菜单使用"
```

---

## Task 17: FileContextMenu 组件 + 接入 FileTable & AppShell

**Files:**
- Create: `src/renderer/src/components/FileContextMenu.tsx`
- Modify: `src/renderer/src/components/FileTable.tsx`
- Modify: `src/renderer/src/components/AppShell.tsx`

- [ ] **Step 1: 创建 `src/renderer/src/components/FileContextMenu.tsx`**

```tsx
import { Dropdown, Modal, message, type MenuProps } from 'antd'
import { useEffect } from 'react'
import { useAppStore } from '@renderer/store/app-store'
import { refreshAll } from '@renderer/api/use-files'

export function FileContextMenu(): JSX.Element | null {
  const target = useAppStore((s) => s.contextMenuTarget)
  const close = useAppStore((s) => s.closeContextMenu)
  const files = useAppStore((s) => s.files)
  const select = useAppStore((s) => s.select)

  // 滚动表格、Esc 时关闭菜单
  useEffect(() => {
    if (!target) return
    function onScroll(): void {
      close()
    }
    function onKey(e: KeyboardEvent): void {
      if (e.key === 'Escape') close()
    }
    window.addEventListener('scroll', onScroll, true)
    window.addEventListener('keydown', onKey)
    return () => {
      window.removeEventListener('scroll', onScroll, true)
      window.removeEventListener('keydown', onKey)
    }
  }, [target, close])

  if (!target) return null
  const file = files.find((f) => f.id === target.id)
  if (!file) return null

  const items: MenuProps['items'] = [
    {
      key: 'open',
      label: '打开文件',
      onClick: () => {
        void window.api.file.open(file.id)
        close()
      }
    },
    {
      key: 'reveal',
      label: '在文件夹中定位',
      onClick: () => {
        void window.api.file.showInDir(file.id)
        close()
      }
    },
    { type: 'divider' },
    {
      key: 'copy-name',
      label: '复制文件名',
      onClick: async () => {
        await navigator.clipboard.writeText(file.name)
        message.success('已复制文件名')
        close()
      }
    },
    {
      key: 'copy-path',
      label: '复制路径',
      onClick: async () => {
        const abs = await window.api.file.getAbsolutePath(file.id)
        if (abs) {
          await navigator.clipboard.writeText(abs)
          message.success('已复制路径')
        }
        close()
      }
    },
    { type: 'divider' },
    {
      key: 'edit-tags',
      label: '编辑标签',
      onClick: () => {
        select(file.id)
        close()
      }
    },
    { type: 'divider' },
    {
      key: 'delete',
      label: '删除',
      danger: true,
      onClick: () => {
        close()
        Modal.confirm({
          title: '删除文件',
          content: `确认删除「${file.name}」？此操作会同时移除磁盘文件。`,
          okText: '删除',
          okButtonProps: { danger: true },
          cancelText: '取消',
          onOk: async () => {
            await window.api.file.delete([file.id])
            useAppStore.getState().select(null)
            await refreshAll()
          }
        })
      }
    }
  ]

  return (
    <Dropdown
      menu={{ items }}
      open
      onOpenChange={(open) => {
        if (!open) close()
      }}
    >
      {/* 1×1 fixed 锚点，用 left/top 把菜单定位到鼠标坐标 */}
      <span
        data-testid="ctx-anchor"
        style={{
          position: 'fixed',
          left: target.x,
          top: target.y,
          width: 1,
          height: 1
        }}
      />
    </Dropdown>
  )
}
```

- [ ] **Step 2: 改 FileTable.tsx —— `onRow` 增加 onContextMenu**

打开 `src/renderer/src/components/FileTable.tsx`，找到 `onRow={(record) => ({...})}` 的对象，把它扩展为：

```tsx
      onRow={(record) => ({
        onClick: () => select(record.id),
        onDoubleClick: async () => {
          const ok = await window.api.file.existsOnDisk(record.id)
          if (!ok) {
            const { message } = await import('antd')
            message.error('文件已丢失，无法打开')
            return
          }
          void window.api.file.open(record.id)
        },
        onContextMenu: (e: React.MouseEvent) => {
          e.preventDefault()
          select(record.id)
          useAppStore
            .getState()
            .openContextMenu({ id: record.id, x: e.clientX, y: e.clientY })
        }
      })}
```

(`React.MouseEvent` 需要顶部 `import type React from 'react'`，已存在则不重复。)

- [ ] **Step 3: 在 AppShell.tsx 挂 FileContextMenu**

打开 `src/renderer/src/components/AppShell.tsx`，在 import 区加：

```ts
import { FileContextMenu } from './FileContextMenu'
```

在 `<UpdateNotifier />` 之后插入：

```tsx
      <FileContextMenu />
```

- [ ] **Step 4: typecheck + test:run**

```bash
npm run typecheck && npm run test:run
```

Expected: 全绿。

- [ ] **Step 5: 提交**

```bash
git add src/renderer/src/components/FileContextMenu.tsx src/renderer/src/components/FileTable.tsx src/renderer/src/components/AppShell.tsx
git commit -m "feat(context-menu): 文件行右键菜单（打开/定位/复制/编辑标签/删除）"
```

---

## Task 18: E2E 测试 —— 右键菜单 & 键盘快捷键

**Files:**
- Create: `tests/e2e/context-menu.spec.ts`
- Create: `tests/e2e/shortcuts.spec.ts`

- [ ] **Step 1: 创建 `tests/e2e/context-menu.spec.ts`**

```ts
import { test, expect } from './fixtures'

test.describe('context menu', () => {
  test('right-click file row opens menu; copy filename writes clipboard', async ({
    page,
    app,
    writeSource
  }) => {
    const src = await writeSource('ctx-target.pdf', 'data')

    await app.evaluate(async ({ dialog }, p) => {
      dialog.showOpenDialog = async () =>
        ({ canceled: false, filePaths: [p] }) as never
    }, src)

    await page.getByRole('button', { name: '导入文件' }).click()
    const row = page.getByTestId('file-table').getByText('ctx-target.pdf')
    await expect(row).toBeVisible()

    // 右键唤起菜单
    await row.click({ button: 'right' })

    // 菜单项可见
    await expect(page.getByText('打开文件')).toBeVisible()
    await expect(page.getByText('在文件夹中定位')).toBeVisible()
    await expect(page.getByText('复制文件名')).toBeVisible()
    await expect(page.getByText('复制路径')).toBeVisible()

    // 点 "复制文件名" → 应弹出成功 toast
    await page.getByText('复制文件名').click()
    await expect(page.getByText('已复制文件名')).toBeVisible()
  })

  test('Esc closes the context menu', async ({
    page,
    app,
    writeSource
  }) => {
    const src = await writeSource('esc-test.pdf', 'data')
    await app.evaluate(async ({ dialog }, p) => {
      dialog.showOpenDialog = async () =>
        ({ canceled: false, filePaths: [p] }) as never
    }, src)
    await page.getByRole('button', { name: '导入文件' }).click()

    const row = page.getByTestId('file-table').getByText('esc-test.pdf')
    await row.click({ button: 'right' })
    await expect(page.getByText('复制文件名')).toBeVisible()

    await page.keyboard.press('Escape')
    // Dropdown 关闭后菜单项不再可见
    await expect(page.getByText('复制文件名')).toHaveCount(0)
  })
})
```

- [ ] **Step 2: 创建 `tests/e2e/shortcuts.spec.ts`**

```ts
import { test, expect } from './fixtures'

test.describe('keyboard shortcuts', () => {
  test('Ctrl+F focuses search input', async ({ page }) => {
    await page.keyboard.press('Control+f')
    const input = page.getByTestId('search-input')
    await expect(input).toBeFocused()
  })

  test('"/" focuses search input when not in input', async ({ page }) => {
    // 先点空白处确保焦点不在 input
    await page.getByTestId('file-table').click()
    await page.keyboard.press('/')
    const input = page.getByTestId('search-input')
    await expect(input).toBeFocused()
  })

  test('Esc clears keyword when no selection', async ({
    page,
    app,
    writeSource
  }) => {
    // 准备 1 个文件
    const src = await writeSource('shortcut.pdf', 'data')
    await app.evaluate(async ({ dialog }, p) => {
      dialog.showOpenDialog = async () =>
        ({ canceled: false, filePaths: [p] }) as never
    }, src)
    await page.getByRole('button', { name: '导入文件' }).click()
    await expect(
      page.getByTestId('file-table').getByText('shortcut.pdf')
    ).toBeVisible()

    // 输入关键词
    await page.getByTestId('search-input').fill('xxx')
    // 等待 debounce
    await expect(page.getByTestId('empty-no-match')).toBeVisible()

    // Esc 清空 keyword（应回到原列表）
    await page.keyboard.press('Escape')
    await expect(
      page.getByTestId('file-table').getByText('shortcut.pdf')
    ).toBeVisible()
  })
})
```

- [ ] **Step 3: 跑 E2E（含新加的两个 spec）**

```bash
unset ELECTRON_RUN_AS_NODE
npm run test:e2e
```

Expected: 4 个老 E2E + 5 个新 E2E = 9 个全过。

> **可能要修的小问题**：context-menu spec 里 `await row.click({ button: 'right' })` 触发的 onContextMenu 会调 `useAppStore.openContextMenu({...x, y})`——如果 Playwright 触发的合成事件 `e.clientX/Y` 为 0，菜单会跑到屏幕左上角。这不影响"菜单是否渲染"的断言（`getByText('复制文件名')` 仍能命中 DOM），E2E 仍应通过。若 antd Dropdown 因坐标问题不渲染，把锚点 div 的 `left/top` clamp 到 `Math.max(target.x, 1)` / `Math.max(target.y, 1)` 即可。先按 spec 跑，遇到问题再修。

- [ ] **Step 4: 提交**

```bash
git add tests/e2e/context-menu.spec.ts tests/e2e/shortcuts.spec.ts
git commit -m "test(e2e): 右键菜单 + 键盘快捷键 5 个 E2E 用例"
```

---

## Task 19: README 加"键盘快捷键"小节

**Files:**
- Modify: `README.md`

- [ ] **Step 1: 在 README "当前进度" 段落之前插入"键盘快捷键"小节**

打开 `README.md`，找到 "## 当前进度" 那一行。在它之前插入：

```markdown
## 键盘快捷键

| 按键 | 行为 |
|------|------|
| `Ctrl+F` / `/` | 聚焦搜索框 |
| `Esc` | 关抽屉 → 清搜索 → 重置筛选（三级回退） |
| `Ctrl+I` | 触发导入流程 |
| `Delete` | 删除当前选中文件（带确认） |
| `Enter` | 打开当前选中文件 |
| `↑` / `↓` | 在列表上下移动选中行 |

输入框内除 `Esc` 外不触发全局快捷键。

```

- [ ] **Step 2: 把"当前进度"勾选 0.2.0**

把：

```markdown
- [x] Part 3：electron-builder 打包 + electron-updater 自动更新
```

后追加一行：

```markdown
- [x] 0.2.0：UI/UX 打磨（中性主题 + 空状态/快捷键/右键菜单 + 移除默认菜单条）
```

- [ ] **Step 3: 提交**

```bash
git add README.md
git commit -m "docs: README 新增键盘快捷键说明；标记 0.2.0 进度"
```

---

## Task 20: 全套回归 + 手动冒烟清单

**Files:** —— 无（仅验证）

- [ ] **Step 1: 全套自动门禁**

```bash
unset ELECTRON_RUN_AS_NODE
npm run lint
npm run format:check
npm run typecheck
npm run test:run
npm run test:e2e
```

Expected:
- lint：0 error（warning 不阻塞）
- format:check：All matched files use Prettier code style!
- typecheck：exit 0
- test:run：45 通过（35 原 + 6 derive-empty-state + 4 derive-esc-action）
- test:e2e：9 通过（4 原 + 2 context-menu + 3 shortcuts）

如有失败，回到对应 task 修。

- [ ] **Step 2: 启动 dev 模式手动检查 6 项**

```bash
npm run dev
```

打开后人眼确认：

1. **菜单条已消失**：窗口顶部 File/Edit/View 菜单不再出现
2. **F12 仍能开 DevTools**（dev 模式保留）
3. **零文件状态**：选一个空仓库目录后，列表区显示 "还没有文件" + "导入第一个文件" 按钮
4. **筛选无结果**：在搜索框输入 "xxxnomatch"，列表显示 "没有匹配的文件" + 当前筛选条件 + "清除筛选" 链接
5. **右键菜单**：导入几个文件后，文件行右键，7 个菜单项可见；点 "复制文件名" 后系统剪贴板里有该名字
6. **快捷键**：Ctrl+F 聚焦搜索，输入后 Esc 清空；选中文件后按 ↑/↓ 切换选中行

- [ ] **Step 3: 打包 + 安装包冒烟**

```bash
npm run package
```

安装产物 `release/0.2.0/smartDoc-0.2.0-setup.exe`，启动后再确认：

7. **打包后菜单条消失**且 F12 无效（生产模式下注册）
8. **窗口加载正常**，可导入、可看详情、可右键、自动更新流程不报错（日志在 `%APPDATA%\smartDoc\logs\main.log`）

- [ ] **Step 4: 提交（无文件改动则跳过）**

如果手动冒烟中发现需修的小问题，分别提交对应 task 的修复 commit。

---

## Task 21: 收尾 —— 合并、tag、发布

**Files:** —— 无（git 操作）

- [ ] **Step 1: 切回 master 合并分支**

```bash
git checkout master
git pull origin master
git merge --no-ff feat/0.2.0-ux -m "Merge 0.2.0: UI/UX 打磨

- 中性主题 token + 干净克制配色
- 空状态/加载态/无结果三种 UI
- 6 个键盘快捷键（Ctrl+F / Esc / Delete / Ctrl+I / Enter / 上下键）
- 文件行右键菜单（7 项）
- 移除 Electron 默认菜单条；dev 模式保留 DevTools 快捷键
- 复活 file:showInDir，新增 file:getAbsolutePath IPC

45 单测 + 9 E2E 全绿。"
```

- [ ] **Step 2: 全套门禁在 master 上再跑一遍**

```bash
npm run lint && npm run format:check && npm run typecheck && npm run test:run
```

Expected: 全绿。

- [ ] **Step 3: 推 master**

```bash
git push origin master
```

- [ ] **Step 4: 打 v0.2.0 tag 并推送（触发 GitHub Actions）**

```bash
git tag -a v0.2.0 -m "smartDoc v0.2.0

UI/UX 打磨：中性主题、空状态、键盘快捷键、右键菜单、移除默认菜单条。
框架不变，业务逻辑零改动。"
git push origin v0.2.0
```

- [ ] **Step 5: 删除特性分支（可选）**

```bash
git branch -d feat/0.2.0-ux
git push origin --delete feat/0.2.0-ux 2>/dev/null || true
```

- [ ] **Step 6: 在 GitHub Actions 上确认 release.yml 跑过**

打开 https://github.com/codeless-boy/smartDoc/actions ，看 v0.2.0 触发的 Release workflow 是否绿灯。如失败，按之前 v0.1.0 的经验排查（最可能是 better-sqlite3 ABI / format:check 行尾，已在 0.1.0 修过）。

- [ ] **Step 7: 在 GitHub Releases 页面确认产物**

打开 https://github.com/codeless-boy/smartDoc/releases/tag/v0.2.0 ，确认：
- `smartDoc-0.2.0-setup.exe` 可下载
- `latest.yml` 存在（用于 electron-updater）
- `.blockmap` 存在

---

## 自查清单

- 设计文档第 2 节（主题 token 体系） → Task 2 落实
- 第 3 节组件视觉打磨（6 个组件） → Tasks 5-10 落实（TagChip / FileTable / SidePanel / DropZone / DuplicateDialog / FileDrawer）
- 第 4 节去除默认菜单条 → Task 4 落实
- 第 5 节空状态 → Tasks 12 / 13 落实（纯函数 + 组件 + 接入）
- 第 6 节键盘快捷键 → Tasks 14 / 15 落实（纯函数 + hook）
- 第 7 节右键菜单 → Tasks 16 / 17 落实（store 扩展 + 组件 + 接入）
- 第 8 节测试与回归 → 各 task 内嵌门禁 + Task 20 全套回归 + Task 18 新 E2E
- 第 10 节文件清单 → 文件结构表已覆盖

**未在计划内但 spec 也未要求的项**：暗色主题、批量操作、布局结构调整、文件预览、MD5 字段、§8 边界场景。spec 明确写"不做"。

**版本号位置**：Task 1 升 package.json；电子构建器 `electron-builder.yml` 没硬编码版本（用 `${version}`），无需手动改。

**git 操作的注意点**：master 是公开历史（远程已有 0.1.0 tag），本次不重写历史；分支只做 fast-forward / `--no-ff` 合并。


</content>
<file_path>e:\code\smartDoc\docs\superpowers\plans\2026-06-22-smartdoc-0.2.0-ux-plan.md</file_path>
